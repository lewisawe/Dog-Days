# Local development (run the app against your Snowflake account or the DuckDB mirror).
# Plain connector (no [pandas] extra / pyarrow) so it installs cleanly on
# Streamlit Community Cloud. The app builds DataFrames from the cursor directly.
streamlit
altair
pandas
snowflake-connector-python
duckdb
